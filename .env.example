# Public production origin, without a trailing slash
VITE_SITE_URL=https://www.yourdomain.com
# Also set SITE_URL in the build environment so sitemap/static HTML use the same origin
SITE_URL=https://www.yourdomain.com
VITE_CONTACT_EMAIL=hello@yourdomain.com
