import { Navigate, useParams } from 'react-router-dom'
import { Breadcrumbs } from '../components/Breadcrumbs'
import { Icon } from '../components/Icon'
import { SEO } from '../components/SEO'
import { ToolCard } from '../components/ToolCard'
import { getCategory, toolsInCategory } from '../data/registry'

export function CategoryPage(){const{slug=''}=useParams();const category=getCategory(slug);if(!category)return <Navigate to="/404" replace/>;const list=toolsInCategory(category.name);return <main className="container-page py-8 sm:py-12"><SEO title={`${category.name} Tools — Free Online Calculators | ToolNest`} description={`${category.description} Browse free ${category.name.toLowerCase()} tools that work instantly in your browser.`} path={`/category/${category.slug}`}/><Breadcrumbs items={[{label:'Home',to:'/'},{label:'Tools',to:'/tools'},{label:category.name}]}/><div className="flex max-w-3xl items-start gap-4"><span className="grid h-14 w-14 shrink-0 place-items-center rounded-2xl bg-brand-50 text-brand-600 dark:bg-brand-700/20 dark:text-brand-100"><Icon name={category.icon} size={27}/></span><div><h1 className="text-3xl font-extrabold tracking-tight sm:text-4xl">{category.name} Tools</h1><p className="mt-2 text-lg leading-7 text-ink-600 dark:text-slate-300">{category.description}</p></div></div>{list.length?<div className="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{list.map(tool=><ToolCard key={tool.slug} tool={tool}/>)}</div>:<div className="card mt-10 p-10 text-center"><h2 className="text-xl font-bold">Everyday tools are being carefully prepared</h2><p className="mx-auto mt-3 max-w-lg text-ink-600 dark:text-slate-300">We only publish complete, useful tools—not placeholder pages. Explore the current directory while this category grows.</p><a className="btn-primary mt-6" href="/tools">Browse all current tools</a></div>}</main>}
