import { Icon } from './Icon'
interface Props { value:string; onChange:(value:string)=>void; large?:boolean; autoFocus?:boolean }
export function SearchBox({value,onChange,large=false,autoFocus=false}:Props){return <label className="relative block"><span className="sr-only">Search tools by name, description, category, or keyword</span><Icon name="search" size={large?23:19} className="pointer-events-none absolute left-4 top-1/2 -translate-y-1/2 text-slate-400"/><input type="search" autoFocus={autoFocus} value={value} onChange={e=>onChange(e.target.value)} placeholder="Search tools..." className={`w-full rounded-2xl border border-slate-300 bg-white pl-12 pr-4 shadow-sm placeholder:text-slate-400 hover:border-slate-400 focus:border-brand-500 dark:border-slate-700 dark:bg-slate-900 ${large?'h-16 text-base sm:text-lg':'h-12 text-sm'}`}/></label>}
